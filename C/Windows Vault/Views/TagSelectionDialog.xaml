<Window x:Class="WindowsVault.Views.TagSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="Assign Tags" 
        Height="450" 
        Width="400"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource SystemControlPageBackgroundChromeLowBrush}">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" 
                   Text="Select Tags to Assign" 
                   FontSize="20" 
                   FontWeight="SemiBold" 
                   Margin="0,0,0,16"/>
        
        <TextBlock Grid.Row="1"
                   Text="Select one or more tags to assign to the imported media:"
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                   Margin="0,0,0,16"/>
        
        <Border Grid.Row="2" 
                Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                CornerRadius="8"
                Padding="12"
                Margin="0,0,0,16">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding AvailableTags}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Margin="0,8" 
                                     IsChecked="{Binding IsSelected}"
                                     Content="{Binding Name}">
                                <CheckBox.ContentTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Ellipse Width="16" 
                                                    Height="16" 
                                                    Fill="{Binding DataContext.Color, RelativeSource={RelativeSource AncestorType=CheckBox}}" 
                                                    Margin="0,0,8,0"/>
                                            <TextBlock Text="{Binding}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </CheckBox.ContentTemplate>
                            </CheckBox>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Skip" 
                    Width="100" 
                    Margin="0,0,8,0"
                    Click="SkipButton_Click"/>
            <Button Content="Assign Tags" 
                    Width="120"
                    Style="{StaticResource AccentButtonStyle}"
                    Click="AssignButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
